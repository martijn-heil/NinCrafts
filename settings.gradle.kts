rootProject.name = "NinCrafts"

pluginManagement {
    repositories {
        gradlePluginPortal()
        maven("https://papermc.io/repo/repository/maven-public/")
        jcenter()
        mavenCentral()
        mavenLocal()
    }
}
